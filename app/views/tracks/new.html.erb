<div class="max-w-xl mx-auto bg-white rounded-lg shadow p-8 mt-10">
  <h1 class="text-2xl font-bold mb-6">Add New Track</h1>
  <%= render 'form', track: @track %>
  <div class="mt-8">
    <%= link_to "Back to Tracks", tracks_path, class: "text-blue-600 underline" %>
  </div>
</div>
